# No environment variables required for this starter.
# The funding scanner is read-only and doesn't need a wallet.

# Optional: custom RPC endpoint (defaults to public Monad RPCs)
# RPC_URL=https://your-rpc-endpoint.com
