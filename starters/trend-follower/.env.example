# Required: your wallet private key (with USDC on Monad)
PRIVATE_KEY=your_private_key_here

# Optional: market to trade (default: ETH-USD)
# MARKET=ETH-USD

# Optional: margin per trade in USDC (default: 50)
# MARGIN=50

# Optional: leverage multiplier (default: 5)
# LEVERAGE=5

# Optional: custom RPC endpoint
# RPC_URL=https://your-rpc-endpoint.com
